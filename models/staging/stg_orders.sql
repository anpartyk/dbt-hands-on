-- Typed-stub: returns zero rows so the project parses cleanly.
-- Replace with the real model when the challenge calls for it.
SELECT
    CAST(NULL AS INT)            AS order_id,
    CAST(NULL AS INT)            AS customer_id,
    CAST(NULL AS DECIMAL(10,2))  AS amount,
    CAST(NULL AS VARCHAR(50))    AS status,
    CAST(NULL AS DATE)           AS ordered_date
WHERE 1=0
