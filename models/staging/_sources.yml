version: 2

sources:
  - name: jaffle_shop
    description: "Raw operational data from the jaffle shop business systems."
    schema: "{{ target.schema }}_seed"
    tables:
      - name: raw_customers
